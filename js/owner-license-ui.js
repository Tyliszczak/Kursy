import {clearOwnerSession,licenseCloudApi,loadOwnerSession,ownerLogin,ownerLogout,ownerSetupConfirm,ownerResetConfirm} from './license-cloud-api.js';
import {esc,formatDate,STATUS_LABELS} from './license-view-helpers.js';

let companies=[];
let ownerState=null;
const root=document.querySelector('#ownerApp');
const statusName=company=>STATUS_LABELS[company.license.status]||company.license.status;
const field=(company,key,label,value,type='number',extra='')=>`<label>${label}<input data-company="${esc(company.id)}" data-field="${key}" type="${type}" value="${esc(value??'')}" ${extra}></label>`;
const notice=message=>message?`<div class="notice">${esc(message)}</div>`:'';

function loginView(message=''){
  companies=[];
  root.innerHTML=`<section class="ownerCompany"><h2>Logowanie właściciela systemu</h2><p class="muted">Dane panelu są ukryte do czasu potwierdzenia hasła przez centralny backend. Hasło nie jest zapisane w kodzie ani w pamięci przeglądarki.</p>${notice(message)}<form id="ownerLoginForm" class="ownerFields"><label>E-mail właściciela<input name="email" type="email" autocomplete="username" required autofocus></label><label>Hasło<input name="password" type="password" autocomplete="current-password" minlength="12" required></label><button class="btn primary" type="submit">Zaloguj do mojego panelu</button></form><div class="actions"><button class="btn" id="ownerForgotPassword" type="button">Nie pamiętam hasła</button></div></section>`;
}
function setupView(message=''){
  companies=[];
  root.innerHTML=`<section class="ownerCompany"><h2>Skonfiguruj konto właściciela</h2><p class="muted">Pierwsza konfiguracja jest możliwa wyłącznie dla adresu konta Google, pod którym działa backend Apps Script. Najpierw wyślemy jednorazowy kod, a dopiero po jego potwierdzeniu zapisany zostanie hash nowego hasła.</p>${notice(message)}<form id="ownerSetupRequestForm" class="ownerFields"><label>E-mail właściciela<input name="email" type="email" autocomplete="email" required autofocus></label><button class="btn primary" type="submit">Wyślij kod konfiguracyjny</button></form><div class="actions"><button class="btn" id="ownerBackToLogin" type="button">Wróć do logowania</button></div></section>`;
}
function setupConfirmView(email,message=''){
  root.innerHTML=`<section class="ownerCompany"><h2>Ustaw hasło właściciela</h2><p class="muted">Kod został wysłany na zweryfikowany adres właściciela. Jest ważny 10 minut.</p>${notice(message)}<form id="ownerSetupConfirmForm" class="ownerFields"><input name="email" type="hidden" value="${esc(email)}"><label>Kod z e-maila<input name="code" inputmode="numeric" autocomplete="one-time-code" pattern="[0-9]{6}" minlength="6" maxlength="6" required autofocus></label><label>Nowe hasło<input name="password" type="password" autocomplete="new-password" minlength="12" required></label><label>Powtórz nowe hasło<input name="password2" type="password" autocomplete="new-password" minlength="12" required></label><button class="btn primary" type="submit">Ustaw hasło i zaloguj</button></form></section>`;
}
function resetView(message=''){
  root.innerHTML=`<section class="ownerCompany"><h2>Odzyskaj dostęp</h2><p class="muted">Jeżeli podany adres jest adresem właściciela, wyślemy na niego jednorazowy kod. Panel nie ujawnia, czy dany adres istnieje.</p>${notice(message)}<form id="ownerResetRequestForm" class="ownerFields"><label>E-mail właściciela<input name="email" type="email" autocomplete="email" required autofocus></label><button class="btn primary" type="submit">Wyślij kod resetujący</button></form><div class="actions"><button class="btn" id="ownerBackToLogin" type="button">Wróć do logowania</button></div></section>`;
}
function resetConfirmView(email,message=''){
  root.innerHTML=`<section class="ownerCompany"><h2>Ustaw nowe hasło</h2><p class="muted">Wpisz kod z wiadomości e-mail i nowe hasło. Po zmianie wszystkie wcześniejsze sesje właściciela zostaną unieważnione.</p>${notice(message)}<form id="ownerResetConfirmForm" class="ownerFields"><input name="email" type="hidden" value="${esc(email)}"><label>Kod z e-maila<input name="code" inputmode="numeric" autocomplete="one-time-code" pattern="[0-9]{6}" minlength="6" maxlength="6" required autofocus></label><label>Nowe hasło<input name="password" type="password" autocomplete="new-password" minlength="12" required></label><label>Powtórz nowe hasło<input name="password2" type="password" autocomplete="new-password" minlength="12" required></label><button class="btn primary" type="submit">Zmień hasło i zaloguj</button></form></section>`;
}
function createCompanyView(){return `<section class="ownerCompany"><h2>Dodaj firmę</h2><form id="newCompanyForm" class="ownerFields"><label>Nazwa firmy *<input name="name" required></label><label>E-mail administratora firmy<input name="adminEmail" type="email"></label><label>Długość triala (dni)<input name="trialDays" type="number" min="1" value="14"></label><button class="btn primary" type="submit">Utwórz firmę</button></form></section>`}
function companyView(company){const license=company.license;return `<section class="ownerCompany"><h2>${esc(company.name)}</h2><p><strong>${esc(statusName(company))}</strong> · administratorzy ${company.adminDevices.length}/${license.limits.adminDevices}, kierowcy ${company.drivers.length}/${license.limits.drivers}, urządzenia kierowców ${company.driverDevices.length}/${license.limits.driverDevices}</p><div class="ownerFields">${field(company,'name','Nazwa firmy',company.name,'text','required')}${field(company,'adminEmail','E-mail administratora',company.adminEmail,'email')}${field(company,'trialDays','Trial (dni)',license.trialDays)}${field(company,'adminDevices','Limit urządzeń administratorów',license.limits.adminDevices)}${field(company,'drivers','Limit kierowców',license.limits.drivers)}${field(company,'driverDevices','Limit urządzeń kierowców',license.limits.driverDevices)}${field(company,'monthlyPrice','Cena miesięczna',license.monthlyPrice??'','number','min="0" step="0.01"')}${field(company,'currency','Waluta',license.currency||'PLN','text','maxlength="3"')}</div><p>Start triala: ${formatDate(license.trialStartedAt)} · koniec triala: ${formatDate(license.trialEndsAt)} · licencja płatna do: ${formatDate(license.paidEndsAt)}</p><div class="actions"><a class="btn" href="company.html">Panel firmy</a><button class="btn" data-owner-action="save:${esc(company.id)}">Zapisz dane, limity i cenę</button><button class="btn" data-owner-action="extend:${esc(company.id)}">Przedłuż trial</button><button class="btn" data-owner-action="end:${esc(company.id)}">Zakończ trial</button><button class="btn primary" data-owner-action="paid:${esc(company.id)}">Nadaj licencję płatną</button><button class="btn dangerText" data-owner-action="block:${esc(company.id)}">${license.blocked||license.status==='blocked'?'Odblokuj firmę':'Zablokuj firmę'}</button></div><details><summary>Historia zmian (${company.history.length})</summary>${company.history.slice().reverse().map(entry=>`<div>${formatDate(entry.at)} - ${esc(entry.type)}</div>`).join('')}</details></section>`}
function render(){const logout='<div class="actions" style="justify-content:flex-end"><button class="btn" id="ownerLogout">Wyloguj bezpiecznie</button></div>';root.innerHTML=logout+createCompanyView()+(companies.map(companyView).join('')||'<div class="empty">Nie utworzono jeszcze żadnej firmy.</div>')}
const value=(id,key)=>document.querySelector(`[data-company="${CSS.escape(id)}"][data-field="${key}"]`)?.value??'';
async function refresh(){const result=await licenseCloudApi.ownerSnapshot();companies=result.companies||[];render()}
async function run(task,button){if(button)button.disabled=true;try{await task();await refresh()}catch(error){alert(error.message)}finally{if(button)button.disabled=false}}
async function showEntry(message=''){try{ownerState=await licenseCloudApi.ownerStatus();if(ownerState.configured)loginView(message);else setupView(message||'Konto właściciela nie jest jeszcze skonfigurowane.')}catch(error){loginView(error.message)}}

document.addEventListener('submit',async event=>{
  const form=event.target;
  if(form.id==='ownerLoginForm'){event.preventDefault();const button=form.querySelector('button'),data=new FormData(form);button.disabled=true;try{await ownerLogin(data.get('email'),data.get('password'));form.reset();await refresh()}catch(error){clearOwnerSession();if(error.code==='OWNER_NOT_CONFIGURED')setupView(error.message);else loginView(error.message)}return}
  if(form.id==='ownerSetupRequestForm'){event.preventDefault();const button=form.querySelector('button'),data=new FormData(form),email=String(data.get('email')||'').trim();button.disabled=true;try{await licenseCloudApi.ownerSetupRequest(email);setupConfirmView(email,'Kod został wysłany. Sprawdź pocztę.')}catch(error){setupView(error.message)}return}
  if(form.id==='ownerSetupConfirmForm'){event.preventDefault();const button=form.querySelector('button'),data=new FormData(form),email=data.get('email'),password=data.get('password');if(password!==data.get('password2')){setupConfirmView(email,'Hasła nie są takie same.');return}button.disabled=true;try{await ownerSetupConfirm({email,code:data.get('code'),password});await refresh()}catch(error){setupConfirmView(email,error.message)}return}
  if(form.id==='ownerResetRequestForm'){event.preventDefault();const button=form.querySelector('button'),data=new FormData(form),email=String(data.get('email')||'').trim();button.disabled=true;try{await licenseCloudApi.ownerResetRequest(email);resetConfirmView(email,'Jeżeli adres jest poprawny, kod został wysłany.')}catch(error){resetView(error.message)}return}
  if(form.id==='ownerResetConfirmForm'){event.preventDefault();const button=form.querySelector('button'),data=new FormData(form),email=data.get('email'),password=data.get('password');if(password!==data.get('password2')){resetConfirmView(email,'Hasła nie są takie same.');return}button.disabled=true;try{await ownerResetConfirm({email,code:data.get('code'),password});await refresh()}catch(error){resetConfirmView(email,error.message)}return}
  if(form.id!=='newCompanyForm')return;event.preventDefault();const data=new FormData(form);await run(()=>licenseCloudApi.ownerCreateCompany({name:data.get('name'),adminEmail:data.get('adminEmail'),trialDays:Number(data.get('trialDays'))||14,country:'PL',currency:'PLN'}));form.reset();form.elements.trialDays.value='14';
});
document.addEventListener('click',async event=>{
  if(event.target.closest('#ownerForgotPassword')){resetView();return}
  if(event.target.closest('#ownerBackToLogin')){await showEntry();return}
  const logoutButton=event.target.closest('#ownerLogout');if(logoutButton){logoutButton.disabled=true;try{await ownerLogout()}catch{}finally{companies=[];await showEntry()}return}
  const button=event.target.closest('[data-owner-action]');if(!button)return;const [action,id]=button.dataset.ownerAction.split(':'),company=companies.find(item=>item.id===id);if(!company)return;
  if(action==='save')run(async()=>{await licenseCloudApi.ownerUpdateCompany({companyId:id,name:value(id,'name'),adminEmail:value(id,'adminEmail')});await licenseCloudApi.ownerUpdateLicense({companyId:id,trialDays:Number(value(id,'trialDays')),adminDeviceLimit:Number(value(id,'adminDevices')),driverLimit:Number(value(id,'drivers')),driverDeviceLimit:Number(value(id,'driverDevices')),monthlyPrice:value(id,'monthlyPrice'),currency:value(id,'currency')})},button);
  if(action==='extend'){const days=Number(prompt('O ile dni przedłużyć trial?',company.license.trialDays))||company.license.trialDays;run(()=>licenseCloudApi.ownerExtendTrial(id,days),button)}
  if(action==='end'&&confirm('Zakończyć trial tej firmy?'))run(()=>licenseCloudApi.ownerEndTrial(id),button);
  if(action==='paid'){const days=Number(prompt('Na ile dni nadać licencję?',365))||365;run(()=>licenseCloudApi.ownerGrantPaid(id,days),button)}
  if(action==='block'&&confirm(company.license.blocked||company.license.status==='blocked'?'Odblokować firmę?':'Zablokować firmę?'))run(()=>licenseCloudApi.ownerSetBlocked(id,!(company.license.blocked||company.license.status==='blocked')),button);
});
if(loadOwnerSession())refresh().catch(async()=>{clearOwnerSession();await showEntry()});else showEntry();
